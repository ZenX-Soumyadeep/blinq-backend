version: '3.8'

services:
  mysql-db:
    image: mysql:8.0
    container_name: blinq_database
    environment:
      MYSQL_ROOT_PASSWORD: rootpassword
      MYSQL_DATABASE: blinq_db
    ports:
      - "3306:3306"
    volumes:
      - blinq_db_data:/var/lib/mysql

volumes:
  blinq_db_data: